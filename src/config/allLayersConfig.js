// allLayersConfig.js

import {
    cityLightLineLayer,
    earthquakeLayer,
    unreinforcedBuildingLayer,
    neighborhoodLayer,
    populationLayer,
    earthquakeM6Layer,
    earthquakeM7Layer,
    hospitalLayer,
    fireStationLayer,
} from "../layers";

const allLayersConfig = {
    cityLightLineLayer,
    earthquakeLayer,
    unreinforcedBuildingLayer,
    neighborhoodLayer,
    populationLayer,
    earthquakeM6Layer,
    earthquakeM7Layer,
    hospitalLayer,
    fireStationLayer,
};

export default allLayersConfig;
